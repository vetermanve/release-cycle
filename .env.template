# Конфигурация полигона. Скопируй в .env (make сам копирует, если .env нет).
# Это статические настройки. Рантайм-секреты (токен, id проектов) -> .state/state.env.

# --- Образы ---
GITLAB_IMAGE=gitlab/gitlab-ce:17.5.1-ce.0
RUNNER_IMAGE=gitlab/gitlab-runner:v17.5.3
NGINX_IMAGE=nginx:alpine
CI_TOOLS_IMAGE=ci-tools:latest

# --- GitLab ---
GITLAB_HTTP_PORT=8929
GITLAB_SSH_PORT=2222
GITLAB_ROOT_PASSWORD=Kx7Qm2Zp9Lt4Bv8Rn
# Внутренний URL (в сети контейнеров). Порт должен совпадать с GITLAB_HTTP_PORT.
GITLAB_INTERNAL_URL=http://gitlab:8929

# --- Полигон ---
GROUP=polygon
NETWORK=relcycle_gitlabnet

# --- Jira (mock) ---
JIRA_URL=http://mock-jira
MOCK_JIRA_PORT=8090

# --- Стенды (host-порты) ---
STAND_DEV_PORT=8081
STAND_TEST_PORT=8082
STAND_PREPOD_PORT=8083
STAND_PROD_PORT=8084

# --- Каденс ---
CLOCK_DEFAULT=2026-06-06
