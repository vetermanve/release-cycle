# Какой релиз-поезд сейчас на каждом стенде (GitOps-binding).
# Промоушн = правка значения + commit (через make или GitLab UI) -> CI деплоит.
# Пусто = на стенде ничего. dev собирается автоматически из bt-set (это интеграционный стенд).
test:   ""
prepod: ""
prod:   ""
