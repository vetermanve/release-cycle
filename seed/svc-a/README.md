# svc-a (демо-backend)

Отдаёт статические JSON из `features/`. Каждый БТ добавляет свой файл `features/bt-<N>.json`.
`shared.json` — общий файл для демонстрации merge-конфликта.

Деплой собирает `features/index.json` (список файлов) на стенде. Фронтенд фетчит его.
